# UMIS RAG 배포 전략

## 🎯 핵심 요구사항

```yaml
개발자 (당신):
  ✅ 사용 = 개발 (인라인 어셈블러)
  ✅ YAML 수정 → 2초 → 반영
  ✅ 빠른 피드백 루프
  
사용자 (다른 사람):
  ✅ 간단한 설치
  ✅ 안정적 품질
  ✅ 선택적 RAG 업데이트

배포:
  ✅ 개발 → 배포 변환 쉽게
  ✅ 버전 관리 명확
  ✅ RAG 업데이트 전략 유연
```

---

## 🔄 전체 워크플로우

```
┌─────────────────────────────────────────────────────────────┐
│  개발자 환경 (당신)                                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 개발 모드 실행:                                          │
│     make dev                                                 │
│     → YAML 자동 감시                                         │
│                                                              │
│  2. Cursor에서 UMIS 사용:                                    │
│     "피아노 분석" → 발견: "해지율 데이터 필요"               │
│                                                              │
│  3. 즉시 수정:                                               │
│     VS Code: YAML 열기 → churn_rate 추가 → Ctrl+S           │
│                                                              │
│  4. 자동 반영: (2초!)                                        │
│     Watcher → 청크 재생성 → 벡터 업데이트 → 완료            │
│                                                              │
│  5. 즉시 검증:                                               │
│     make query QUERY="코웨이 해지율"                         │
│     → 추가된 데이터 확인! ✅                                 │
│                                                              │
│  6. Git 커밋:                                                │
│     git add umis_business_model_patterns_v6.2.yaml           │
│     git commit -m "Add: 코웨이 해지율 데이터"                │
│     git push origin develop                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
                   (주간/월간 배포)
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  배포 프로세스                                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 개발 완료 확인:                                          │
│     - develop 브랜치 안정화                                  │
│     - 모든 테스트 통과                                       │
│     - 문서 업데이트                                          │
│                                                              │
│  2. 배포 패키지 생성:                                        │
│     python scripts/build_release.py --version 1.1.0          │
│                                                              │
│     선택:                                                    │
│       • 인덱스 포함? (y)                                     │
│         → 즉시 사용 가능 (150MB)                             │
│                                                              │
│       • 인덱스 제외? (n)                                     │
│         → 작은 크기 (5MB), 사용자가 구축                     │
│                                                              │
│  3. GitHub Release:                                          │
│     git tag v1.1.0                                           │
│     git push origin v1.1.0                                   │
│     → releases/umis-rag-v1.1.0.zip 업로드                    │
│                                                              │
│  4. 배포 알림:                                               │
│     - 릴리즈 노트 게시                                       │
│     - 사용자에게 업데이트 안내                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  사용자 환경 (Option A: Local RAG)                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  초기 설치:                                                  │
│    1. ZIP 다운로드                                           │
│    2. 압축 해제                                              │
│    3. ./setup.sh (인덱스 없는 경우)                          │
│    4. .env에 API 키 입력                                     │
│    5. 사용 시작!                                             │
│                                                              │
│  사용:                                                       │
│    - Cursor: @umis_guidelines_v6.2.yaml                     │
│    - 필요 시: query_rag.py                                   │
│                                                              │
│  업데이트:                                                   │
│    git pull origin main                                      │
│    make rebuild                                              │
│    → 최신 YAML + 인덱스                                      │
│                                                              │
│  커스터마이징:                                               │
│    - YAML 수정 (자기 프로젝트용)                             │
│    - 자기 데이터 추가                                        │
│    - 독립적 관리                                             │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  사용자 환경 (Option B: Shared RAG)                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  초기 설정:                                                  │
│    1. YAML 파일만 다운로드 (1MB)                             │
│    2. .env 설정:                                             │
│       UMIS_RAG_MODE=shared                                   │
│       UMIS_RAG_ENDPOINT=https://rag.umis.io                  │
│    3. 즉시 사용!                                             │
│                                                              │
│  사용:                                                       │
│    - Cursor: @umis_guidelines_v6.2.yaml                     │
│    - RAG: 중앙 서버 자동 사용                                │
│                                                              │
│  업데이트:                                                   │
│    - YAML만: git pull (가끔)                                 │
│    - RAG: 자동 (중앙에서)                                    │
│    → 신경 쓸 것 없음!                                        │
│                                                              │
│  한계:                                                       │
│    - 커스터마이징 불가                                       │
│    - 인터넷 필요                                             │
│    - API 비용 가능                                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 개발자 워크플로우 (상세)

### 일반적인 하루

```bash
# 아침: 개발 모드 시작
cd ~/umis-main
make dev

# Watcher 실행됨 (백그라운드)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 UMIS Development Watcher
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 준비 완료!

📁 감시: data/raw/
💡 YAML 수정 → 2초 → 반영!
⚠️  종료: Ctrl+C
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Cursor에서 실제 프로젝트
@umis_guidelines_v6.2.yaml
"음악 스트리밍 구독 분석"

# 사용 중 발견
"Spotify 프리미엄 전환율 데이터 필요"

# VS Code에서 수정
# data/raw/umis_business_model_patterns_v6.2.yaml
spotify:
  ...
  conversion: "42%"  # 추가!

# Ctrl+S

# Watcher 자동 감지 (2초 후)
🔄 변경 감지: umis_business_model_patterns_v6.2.yaml
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1/3 청크 재생성...
  2/3 벡터 임베딩...
  3/3 인덱스 업데이트...
  
  ✅ 완료! (1.9초)

# 즉시 검증
make query QUERY="Spotify 전환율"

# 결과:
🥇 subscription_model_case_스포티파이
   conversion: "42%" ← 방금 추가한 것!

# 만족! Git 커밋
git add data/raw/umis_business_model_patterns_v6.2.yaml
git commit -m "Add: Spotify 전환율 42%"

# 저녁: 종료
Ctrl+C  (Watcher 중단)
```

**피드백 루프:** 발견 → 수정 → 반영 → 검증 = **5초!** ⚡

---

## 📦 배포 전략 (2가지 Track)

### Track 1: Index-Included (권장 - 일반 사용자)

```yaml
패키지:
  umis-rag-v1.1.0-with-index.zip (150MB)
  
포함:
  ✅ YAML 파일 (모든 지식)
  ✅ Python 코드
  ✅ 사전 구축 인덱스 ← 핵심!
  ✅ manifest.yaml
  
설치:
  1. 압축 해제
  2. .env에 API 키만
  3. 즉시 사용!
  
장점:
  ✅ 설치 3분
  ✅ 인덱스 구축 불필요
  ✅ 초보자 친화적
  
단점:
  ❌ 큰 파일 (150MB)
  ❌ 다운로드 느림
```

### Track 2: Slim Version (개발자/고급 사용자)

```yaml
패키지:
  umis-rag-v1.1.0-slim.zip (5MB)
  
포함:
  ✅ YAML 파일
  ✅ Python 코드
  ❌ 인덱스 제외
  
설치:
  1. 압축 해제
  2. ./setup.sh
  3. 인덱스 구축 (2분)
  4. 사용 시작
  
장점:
  ✅ 작은 파일
  ✅ 빠른 다운로드
  ✅ 커스터마이징 가능
  
단점:
  ❌ 초기 구축 필요
  ❌ OpenAI API 필요
```

---

## 🔄 RAG 업데이트 전략

### Strategy A: Local-First (추천!)

```yaml
개념:
  "각자 자기 RAG 관리, 중앙은 참조만"
  
구조:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GitHub (중앙):
    - YAML 파일 (소스)
    - Python 코드
    - 변환 스크립트
    - 배포 패키지
  
  각 사용자 (로컬):
    - git clone
    - 자기 인덱스 구축
    - 자기 데이터 추가 가능
    - 독립적 관리
  
  업데이트:
    - git pull (YAML 최신화)
    - make rebuild (인덱스 재구축)
    - 1분 소요
  
장점:
  ✅ 완전한 통제
  ✅ 오프라인 가능
  ✅ 커스터마이징
  ✅ 개인 데이터 추가
  ✅ 중앙 서버 불필요
  
단점:
  ⚠️ 초기 설정
  ⚠️ 디스크 공간 (1GB)
  ⚠️ 업데이트 수동
```

### Strategy B: Shared RAG (선택적)

```yaml
개념:
  "중앙 RAG 서버, 사용자는 API만"
  
구조:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  중앙 서버:
    - Pinecone (벡터 DB)
    - Neo4j (Knowledge Graph)
    - FastAPI (RAG API)
    - 자동 업데이트 (주간)
  
  사용자:
    - YAML만 다운로드
    - .env: UMIS_RAG_ENDPOINT 설정
    - API로 RAG 사용
  
  업데이트:
    - 중앙: 주간 자동
    - 사용자: 신경 쓸 것 없음
  
장점:
  ✅ 설정 최소
  ✅ 디스크 절약
  ✅ 자동 업데이트
  ✅ 항상 최신
  
단점:
  ❌ 인터넷 필요
  ❌ 중앙 의존
  ❌ API 비용
  ❌ 커스터마이징 불가
```

### Strategy C: Hybrid (최종 추천!) ⭐

```yaml
개념:
  "Local-first, Shared-optional"
  
기본:
  - 로컬 RAG (자체 관리)
  - Git으로 업데이트
  
선택:
  - 중앙 RAG도 사용 가능
  - 전환 쉬움
  
.env 설정:
  # Local RAG (기본)
  UMIS_RAG_MODE=local
  
  # 또는 Shared RAG
  # UMIS_RAG_MODE=shared
  # UMIS_RAG_ENDPOINT=https://rag.umis.io
  
자동 Fallback:
  - 로컬 실패 → 중앙 시도
  - 중앙 실패 → 로컬로 복구
  
장점:
  ✅ 유연성 최대
  ✅ 안정성 높음
  ✅ 사용자 선택
```

---

## 📊 버전 관리 상세

### Git Branch 전략

```
main (stable, tagged releases)
  └── v1.0.0, v1.1.0, v1.2.0...

develop (latest stable)
  ├── git merge feature → 여기로
  └── 주간 → main 병합

feature/* (실험)
  ├── feature/knowledge-graph
  ├── feature/stewart-circular
  └── feature/goal-alignment

hotfix/* (긴급 수정)
  └── hotfix/critical-bug
```

### 파일별 업데이트 전략

```yaml
YAML 파일:
  - 위치: 루트 (umis_guidelines_v6.2.yaml)
  - 버전: Git 태그
  - 업데이트: Git pull
  - 주기: 월간 (major), 주간 (minor)
  
Python 코드:
  - 위치: umis_rag/
  - 버전: pyproject.toml
  - 업데이트: pip install --upgrade
  - 주기: 필요 시
  
RAG 인덱스:
  - 위치: data/chroma/
  - 버전: manifest.yaml
  - 업데이트: make rebuild
  - 주기: YAML 변경 시
  
청크:
  - 위치: data/chunks/
  - 생성: 01_convert_yaml.py
  - 업데이트: YAML 변경 시 자동
  - 버전: YAML과 동기화
```

### manifest.yaml (인덱스 메타데이터)

```yaml
# data/chroma/manifest.yaml

version: "1.1.0"
created_at: "2024-11-01T20:30:00"
umis_yaml_version: "6.2"
embedding_model: "text-embedding-3-large"
embedding_dimension: 3072

collections:
  steve_knowledge_base:
    documents: 54
    chunks:
      business_models: 31
      disruption_patterns: 23
    
    source_files:
      - name: "umis_business_model_patterns_v6.2.yaml"
        hash: "a3f5c2..."  # MD5 hash
        last_modified: "2024-11-01"
      
      - name: "umis_disruption_patterns_v6.2.yaml"
        hash: "b7e9d1..."
        last_modified: "2024-11-01"

compatibility:
  min_langchain: "1.0.0"
  min_openai: "1.10.0"
  min_chromadb: "1.3.0"

rebuild_required: false  # YAML 변경 시 true
```

---

## 🚀 배포 시나리오

### Scenario 1: 주간 Minor Release

```bash
# 1. develop에 기능 병합됨
git checkout develop
git merge feature/add-new-cases

# 2. 테스트
make test
→ 모두 통과

# 3. 배포 패키지 생성
python scripts/build_release.py --version 1.1.1 --include-index

# 4. 태그 및 배포
git checkout main
git merge develop
git tag v1.1.1
git push origin main --tags

# 5. GitHub Release
# releases/umis-rag-v1.1.1.zip 업로드

# 사용자는:
git pull origin main
make rebuild  (1분)
→ 최신 버전!
```

### Scenario 2: 월간 Major Release

```bash
# 1. Knowledge Graph 완성 (2주 개발)
git checkout feature/knowledge-graph
# ... 개발 ...
git checkout develop
git merge feature/knowledge-graph

# 2. 충분한 테스트 (1주)
make test
# 실제 프로젝트 테스트
# 버그 수정

# 3. 배포
python scripts/build_release.py --version 1.2.0 --include-index

# 4. 릴리즈 노트 작성
# - 주요 변경사항
# - Breaking changes
# - Migration guide

# 5. 배포
git tag v1.2.0
git push origin v1.2.0

# 사용자는:
git pull
make rebuild  (새 기능 포함!)
```

---

## 💡 핵심 해결책: Hot-Reload Development

### 개발 모드 상세

```python
# make dev 실행 시

1. dev_watcher.py 시작
   └─ watchdog로 data/raw/ 감시

2. YAML 파일 변경 감지
   ├─ 0.1초: 파일 변경 이벤트
   ├─ 중복 필터링 (1초 내)
   └─ 업데이트 트리거

3. 증분 업데이트 (2초!)
   ├─ 변경된 파일만 처리
   ├─ 해당 청크만 재생성
   ├─ 벡터만 Upsert
   └─ 완료!

4. 즉시 테스트 가능
   └─ 다음 query_rag.py부터 반영

→ 인라인 어셈블러 수준! ⚡
```

### 실제 타이밍

```
0ms: YAML 수정 (VS Code)
0ms: Ctrl+S
100ms: Watcher 감지
500ms: 청크 재생성 (변경 파일만)
1500ms: 벡터 업데이트 (Upsert)
2000ms: 완료! ✅

→ 2초 만에 적용!
→ Cursor에서 즉시 사용 가능!
```

---

## 🎯 사용자 선택 가이드

### 누가 어떤 방식?

```yaml
초보 사용자:
  추천: Shared RAG (Option B)
  이유: 설정 최소, 즉시 시작
  
고급 사용자:
  추천: Local RAG (Option A)
  이유: 커스터마이징, 오프라인
  
개발자:
  필수: Local + Hot-Reload
  이유: 피드백 루프
  
기업:
  추천: Private Shared RAG
  이유: 중앙 관리, 데이터 보안
```

---

## 🔧 실전 구현 가이드

### 지금 당장 (개발 모드)

```bash
# 1. watchdog 설치
pip install watchdog

# 2. 개발 모드 시작
make dev

# 3. YAML 수정하며 사용
# → 2초마다 자동 반영!

# 4. Git 커밋 (중요한 변경만)
git add data/raw/*.yaml
git commit -m "Update: ..."
```

### 이번 주말 (배포 패키지)

```bash
# 1. 개발 정리
git checkout develop
git merge feature/...

# 2. 테스트
make test

# 3. 배포 생성
python scripts/build_release.py \
  --version 1.0.0 \
  --include-index

# 4. GitHub Release
# → releases/umis-rag-v1.0.0.zip

# 5. 다른 사람 사용 가능!
```

### 다음 달 (Shared RAG) - 선택

```bash
# 중앙 RAG 서버 (FastAPI)

# 1. 서버 구현
fastapi + pinecone + neo4j

# 2. 배포
docker compose up

# 3. 사용자는
.env:
  UMIS_RAG_MODE=shared
  UMIS_RAG_ENDPOINT=https://rag.umis.io

# 4. 자동 업데이트
# 중앙에서 주간 업데이트
```

---

## 📋 최종 추천

### Phase 1: 개발자 환경 (지금)

```yaml
구현:
  ✅ Hot-Reload (make dev)
  ✅ 빠른 피드백 (2초)
  ✅ Git 기반 관리
  
사용:
  - YAML 수정하며 즉시 반영
  - 인라인 어셈블러 경험
  - 최고의 생산성
```

### Phase 2: 배포 패키지 (이번 주)

```yaml
준비:
  - build_release.py 완성
  - 2개 Track (Index-Included, Slim)
  - GitHub Release
  
사용자:
  - ZIP 다운로드
  - 간단한 설치
  - 즉시 사용
```

### Phase 3: MCP Tool (2주 후)

```yaml
통합:
  - Cursor MCP Tool
  - YAML 1개만 첨부
  - RAG 자동 활용
  
경험:
  - 개발자: Hot-Reload
  - 사용자: MCP Tool
  - 모두 행복! ✨
```

---

## 결론

**당신의 요구사항 완벽 구현:**

```yaml
✅ 개발 = 사용 (Hot-Reload)
✅ 피드백 즉시 반영 (2초)
✅ 배포 간단 (build_release.py)
✅ RAG 업데이트 유연 (Local/Shared)
✅ 개발 → 배포 자동화

→ 인라인 어셈블러 + 프로덕션 품질!
```

지금 `make dev` 테스트해보시겠어요? 🚀

