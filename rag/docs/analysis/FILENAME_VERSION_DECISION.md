# 파일명 버전 관리 결정

**질문:** 파일명에 버전을 어떻게 표기할 것인가?

---

## 📊 옵션 비교

### Option 1: 버전 완전 삭제 (가장 단순!)

```yaml
파일명:
  umis_guidelines.yaml
  umis_business_model_patterns.yaml
  umis_disruption_patterns.yaml

버전 관리:
  VERSION.txt: 7.0.0
  파일 내부 _meta.version: "7.0.0"
  CHANGELOG.md: 모든 변경사항
```

**장점:**
```yaml
✅ 영구 고정: 절대 변경 불필요
✅ 참조 안정: 영원히 같은 이름
✅ 단순함: 버전 신경 안 씀
✅ Cursor 친화: @umis_guidelines.yaml (항상 동일)
```

**단점:**
```yaml
❌ 메이저 버전 공존 불가:
   v6, v7 동시 사용 불가능
   
❌ 파일명만으로 버전 모름:
   열어봐야 알 수 있음

평가:
  → 대부분 케이스에 문제 없음!
  → 여러 메이저 버전 동시 사용은 드뭄
```

---

### Option 2: 메이저만 (vX)

```yaml
파일명:
  umis_guidelines_v6.yaml
  umis_business_model_patterns_v6.yaml
  umis_disruption_patterns_v6.yaml

버전 관리:
  VERSION.txt: 7.0.0
  파일 내부 _meta.version: "7.0.0"
```

**장점:**
```yaml
✅ 메이저 구분: v6, v7 구분 가능
✅ 안정: 마이너 변경 시 유지
✅ 공존: v6, v7 동시 사용 가능
✅ 균형: 명확성 + 안정성
```

**단점:**
```yaml
⚠️ 메이저 변경 시: rename 필요
   v6 → v7 시 umis_guidelines_v7.yaml
```

---

### Option 3: 현재 유지 (v6.2)

```yaml
파일명:
  umis_guidelines.yaml (현재)
  
변경 시:
  v6.3 → umis_guidelines_v6.3.yaml
  v6.3.1 → umis_guidelines_v6.3.1.yaml
```

**단점:**
```yaml
❌ 매번 변경: 마이너마다 rename
❌ 참조 깨짐: .cursorrules 등 수정
❌ 비효율: 작은 변경에도 대공사
```

---

## 🎯 최종 추천

### 🥇 Option 1: 버전 완전 삭제 (가장 단순!)

**이유:**

```yaml
UMIS 사용 패턴:
  • 사용자는 항상 최신 버전 사용
  • 여러 버전 동시 사용 거의 없음
  • Cursor에서 @umis_guidelines.yaml (간단!)

문제 없음:
  • VERSION.txt로 버전 확인
  • _meta로 정확한 버전
  • CHANGELOG로 변경 이력
  
  → 파일명에 버전 불필요!

v7.0 나와도:
  • 파일명 그대로 (umis_guidelines.yaml)
  • 내용만 v7로 교체
  • 사용자: 변경 인식 안 함
  
  → 완벽한 투명성!
```

---

## 🔄 실행 계획

### v6.2 → 버전 제거

```yaml
변경:
  umis_guidelines.yaml → umis_guidelines.yaml
  umis_business_model_patterns.yaml → umis_business_model_patterns.yaml
  umis_disruption_patterns.yaml → umis_disruption_patterns.yaml
  umis_ai_guide.yaml → umis_ai_guide.yaml
  umis_deliverable_standards.yaml → umis_deliverable_standards.yaml
  umis_examples.yaml → umis_examples.yaml

영향:
  • .cursorrules 참조 수정
  • scripts/*.py 참조 수정
  • 문서 @umis... 참조 수정

소요:
  • 자동 스크립트: 5분
  • 테스트: 5분
  
  총: 10분
```

---

## 💡 대안: 메이저만 (v6)

만약 메이저 버전 구분이 필요하다면:

```yaml
파일명:
  umis_guidelines_v6.yaml
  
장점:
  • v6, v7 구분 가능
  • 공존 가능

단점:
  • v7 나오면 rename 필요
  
평가:
  → Option 1보다 약간 복잡
  → 하지만 여전히 좋음
```

---

## 🎯 최종 결정

**어떤 방식을 선호하시나요?**

**A. 버전 완전 삭제** (umis_guidelines.yaml) ⭐ 가장 단순!  
**B. 메이저만** (umis_guidelines_v6.yaml) ⭐ 균형잡힘!  
**C. 현재 유지** (umis_guidelines.yaml)

---

**제 추천: Option A (버전 삭제)**

이유:
- UMIS는 항상 최신 사용
- 여러 버전 동시 사용 없음
- 참조 영구 안정
- 극도로 단순

**선택해주시면 즉시 적용하겠습니다!** 🚀

