# Claude API 설정 가이드 (2025-11-20)

## ✅ 완료된 작업

1. **anthropic 패키지 설치 완료**
   - 버전: 0.74.1
   - 설치 위치: Python 3.13

2. **requirements.txt 업데이트**
   - anthropic>=0.40.0 추가

3. **env.template 업데이트**
   - ANTHROPIC_API_KEY 항목 추가

## 🔑 다음 단계: .env 파일 업데이트

### 1. Anthropic API 키 발급

1. https://console.anthropic.com/ 접속
2. 로그인 또는 회원가입
3. API Keys 메뉴로 이동
4. "Create Key" 클릭
5. API 키 복사 (sk-ant-로 시작)

### 2. .env 파일에 추가

.env 파일을 열고 다음 줄을 추가하세요:

```bash
# Anthropic (Claude) API 키
ANTHROPIC_API_KEY=sk-ant-your-actual-key-here
```

**위치**: OPENAI_API_KEY 바로 아래에 추가하면 됩니다.

### 3. 예시

```bash
# ========================================
# 🔑 API Keys
# ========================================

# OpenAI API 키 (필수!)
OPENAI_API_KEY=sk-proj-your-openai-key-here

# Anthropic (Claude) API 키 (선택, 벤치마크용)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
```

## 🧪 테스트

.env 파일 업데이트 후 다음 명령으로 테스트:

```bash
python3 -c "import os; from dotenv import load_dotenv; load_dotenv(); print('OpenAI:', 'OK' if os.getenv('OPENAI_API_KEY') else 'Missing'); print('Anthropic:', 'OK' if os.getenv('ANTHROPIC_API_KEY') else 'Missing')"
```

예상 결과:
```
OpenAI: OK
Anthropic: OK
```

## 🚀 벤치마크 실행

API 키 설정 완료 후:

```bash
cd /Users/kangmin/umis_main_1103/umis
python3 scripts/benchmark_llm_models_2025.py
```

### 옵션 선택

실행 시 다음 선택지가 나타납니다:

1. **전체 모델 테스트** (25개 모델, ~30분, ~$10-20)
2. **핵심 모델만 테스트** (6개 모델, ~15분, ~$2-5) ⭐ 권장!
3. **커스텀 선택**

### 핵심 모델 (권장)

- **OpenAI**: gpt-4o-mini, gpt-4o, o1-mini
- **Claude**: claude-haiku-4.5, claude-sonnet-4.5, claude-opus-4.1

각 모델이 8개 시나리오 (Phase 0-4)를 테스트합니다.

## 💰 예상 비용

### 핵심 모델 테스트 (6개 × 8개 시나리오 = 48회)

| 모델 | 8회 호출 예상 비용 |
|------|------------------|
| gpt-4o-mini | $0.004 |
| gpt-4o | $0.10 |
| o1-mini | $0.072 |
| claude-haiku-4.5 | $0.028 |
| claude-sonnet-4.5 | $0.084 |
| claude-opus-4.1 | $0.42 |
| **합계** | **~$0.70** |

### 전체 모델 테스트 (25개 × 8개 시나리오 = 200회)

예상 총 비용: **$5-10**

## 📊 출력 결과

### 1. 실시간 진행 상황

```
================================================================================
시나리오 1/8: Phase 0 (Literal Lookup)
================================================================================

📦 openai_mini

   ✅ gpt-4o-mini
      비용: $0.000450
      시간: 2.3초
      토큰: 450 (350→100)
      품질: 100/100
      답변: 200000 원
      신뢰도: 1.0
```

### 2. JSON 결과 파일

`benchmark_results_YYYYMMDD_HHMMSS.json`
- 모든 테스트 결과 저장
- 메타데이터 포함
- 품질 점수, 비용, 시간 등

### 3. 종합 리포트

```
================================================================================
📊 벤치마크 리포트 (2025-11-20)
================================================================================

총 테스트: 48개
성공: 48개 (100.0%)
실패: 0개

================================================================================
Phase별 최적 모델
================================================================================

🔹 Phase 0
   TOP 3 (품질 기준):
   1위: gpt-4o-mini              | 품질: 100/100 | $0.000450 | 2.3초
   2위: claude-haiku-4.5         | 품질: 100/100 | $0.003500 | 1.8초
   3위: claude-sonnet-4.5        | 품질: 100/100 | $0.010500 | 2.1초

...

🏆 최종 권장 (UMIS용)
💎 최고 가성비 TOP 5:
   1위: 🔵 gpt-4o-mini              | 가성비: 222.2 | 품질: 100.0/100 | 비용: $0.000450
   2위: 🟣 claude-haiku-4.5         | 가성비: 28.6 | 품질: 100.0/100 | 비용: $0.003500
   ...
```

## ⚠️ 주의사항

1. **API 요금 주의**
   - 테스트는 실제 API 호출이므로 비용이 발생합니다
   - 핵심 모델 테스트 권장 (~$0.70)

2. **Rate Limit**
   - 각 호출 사이 1초 대기 (자동)
   - 전체 테스트 약 15-20분 소요

3. **실패 처리**
   - 일부 모델이 아직 API에 없을 수 있음 (gpt-5-pro 등)
   - 실패는 자동으로 기록되고 건너뜁니다

4. **중단 시**
   - Ctrl+C로 중단 가능
   - 부분 결과 저장 옵션 제공

## 🎯 다음 단계

1. **.env 파일에 ANTHROPIC_API_KEY 추가**
2. **테스트 실행**: `python3 scripts/benchmark_llm_models_2025.py`
3. **옵션 2 선택** (핵심 모델만)
4. **결과 분석**: JSON 파일 + 리포트
5. **가격 비교 문서 업데이트**: COMPLETE_LLM_MODEL_COMPARISON.md

---

**작성일**: 2025-11-20
**대상**: UMIS v7.7.0+
**필요 패키지**: anthropic>=0.40.0 ✅

