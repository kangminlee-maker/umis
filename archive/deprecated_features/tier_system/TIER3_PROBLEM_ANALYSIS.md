# Tier 3 문제 분석 보고서

**작성일**: 2025-11-10  
**작성자**: AI Assistant  
**버전**: v7.5.0

---

## 📋 요약

Tier 3 Fermi 분해 시스템이 Native Mode에서 **설계대로 동작하지만 실제로는 작동하지 않는 문제**가 발견되었습니다.

**핵심 문제**: Native Mode에서 Tier 3가 모형 생성을 건너뛰고 "Cursor에게 맡긴다"고 하지만, 실제로 Cursor가 자동으로 Fermi 분해를 수행하지는 않습니다.

---

## 🔍 발견된 문제들

### 1. **Native Mode에서 모형 생성 실패** (심각도: ⚠️⚠️⚠️ 높음)

#### 문제 상황
```python
# tier3.py Line 764-768
elif self.llm_mode == 'native':
    logger.info(f"{'  ' * depth}    Native Mode → Cursor LLM에게 Fermi 분해 요청")
    logger.info(f"{'  ' * depth}    ℹ️  비즈니스 지표(LTV, CAC 등)는 Quantifier가 처리")
    logger.info(f"{'  ' * depth}    ℹ️  Tier 3는 일반 Fermi 분해만 담당")
    return []  # Native mode에서는 Cursor가 직접 Fermi 분해 수행
```

#### 실제 로그
```
2025-11-10 08:21:55 | INFO | tier3:_generate_default_models:765 - Native Mode → Cursor LLM에게 Fermi 분해 요청
2025-11-10 08:21:55 | INFO | tier3:_phase2_generate_models:728 - 생성된 모형: 0개
2025-11-10 08:21:55 | WARNING | tier3:estimate:554 - ❌ Phase 2 실패 (모형 없음)
```

#### 원인
- **설계 의도**: Native Mode에서는 비용을 절약하기 위해 LLM API 호출을 하지 않고, Cursor Native LLM이 직접 Fermi 분해를 수행하도록 "위임"
- **실제 동작**: 빈 리스트(`[]`)를 반환하면 Phase 2가 실패하고, Tier 3 전체가 중단됨
- **근본 원인**: **"위임"이 자동으로 일어나지 않음**. Cursor LLM은 Python 코드 실행 결과만 받을 뿐, Tier 3가 중단되었다는 것을 알 수 없음

#### 영향
- ✅ **v7.5.0 이전**: 비즈니스 지표 템플릿이 있어서 80-90% 커버
- ❌ **v7.5.0**: 비즈니스 지표 템플릿 제거 → 템플릿 없는 질문은 100% 실패
- ❌ **일반 Fermi 질문**: "서울-부산 이동 시간", "여객기 내 탁구공 수" 등 → 모두 실패

---

### 2. **비즈니스 지표 템플릿 제거** (심각도: ⚠️⚠️ 중간)

#### 변경 사항 (v7.5.0)
```python
# tier3.py Line 791-811
def _match_business_metric_template(
    self,
    question: str
) -> List[FermiModel]:
    """
    비즈니스 지표 템플릿 매칭 - DISABLED (v7.5.0)
    
    v7.5.0 변경:
    - 비즈니스 지표 템플릿 제거됨
    - Quantifier가 LTV, CAC 등의 계산 담당
    - Estimator Tier 3는 일반적 Fermi 분해만 수행
    """
    # v7.5.0: 비즈니스 지표 템플릿 제거
    # Quantifier가 비즈니스 지표 계산 담당
    return []
```

#### 원인
- **역할 분리**: Estimator(값 추정) vs Quantifier(계산)
- **의도는 정확**: 비즈니스 지표는 Quantifier가 처리하는 게 맞음
- **부작용**: Tier 3가 처리할 수 있는 케이스가 크게 줄어듦

#### 영향
- ✅ **비즈니스 지표 (LTV, CAC 등)**: Quantifier로 이동 → 정상
- ❌ **일반 Fermi 질문**: 템플릿도 없고, LLM 모형 생성도 안 됨 → 실패
- ❌ **커버리지**: 80-90% → ~0%

---

### 3. **Phase 2 실패 시 Fallback 없음** (심각도: ⚠️⚠️ 중간)

#### 문제 코드
```python
# tier3.py Line 546-555
candidate_models = self._phase2_generate_models(
    question,
    scan_result['available'],
    scan_result['unknown'],
    depth
)

if not candidate_models:
    logger.warning(f"{'  ' * depth}  ❌ Phase 2 실패 (모형 없음)")
    return None  # ← 바로 실패!
```

#### 원인
- Phase 2에서 모형이 없으면 바로 `None` 반환
- Tier 2로 Fallback하거나 다른 대안이 없음

#### 영향
- Native Mode에서 템플릿이 없는 모든 질문이 실패

---

### 4. **Tier 1 Built-in 규칙 파일 없음** (심각도: ⚠️ 낮음)

#### 로그
```
2025-11-10 08:21:13 | WARNING | tier1:_load_builtin_rules:64 - 
  ⚠️  Built-in 규칙 로드 실패: 
  [Errno 2] No such file or directory: 
  '/Users/kangmin/umis_main_1103/umis/umis_rag/data/tier1_rules/builtin.yaml'
```

#### 원인
- 경로가 잘못됨: `umis_rag/data/` → `data/tier1_rules/`

#### 영향
- Tier 1에서 빌트인 규칙을 사용할 수 없음
- 하지만 학습된 규칙 + RAG는 정상 작동
- 심각도 낮음 (기능에 큰 영향 없음)

---

## 🔧 원인 심층 분석

### 설계 의도 vs 실제 동작

#### config/llm_mode.yaml의 정책 (Line 290-321)

```yaml
tier3_policy:
  
  native_mode:
    llm_usage: "사용 안 함 (템플릿만)"
    coverage: "80-90% (8개 지표 템플릿)"
    cost: "$0"
    
    behavior:
      template_match: "템플릿 매칭 성공 → Tier 3 실행"
      template_fail: "Tier 3 중단 → Cursor Native LLM에게 맡김"  # ← 문제!
    
    reason: |
      Native LLM (Cursor)이 더 우수하고 비용 $0
      복잡한 모형 생성은 Cursor가 직접 처리하는게 나음
```

#### 설계 가정 (잘못됨)

1. ❌ **가정**: "Tier 3 중단 → Cursor Native LLM에게 맡김"
2. ❌ **현실**: Tier 3가 중단되면 그냥 실패. Cursor는 아무것도 모름
3. ❌ **오해**: Cursor가 자동으로 Fermi 분해를 수행할 거라고 기대

#### 실제 필요한 동작

1. ✅ **옵션 A**: Native Mode에서도 기본 모형 생성 (템플릿 활용)
2. ✅ **옵션 B**: Phase 2 실패 시 Tier 2로 Fallback
3. ✅ **옵션 C**: Native Mode에서 간단한 규칙 기반 모형 생성

---

## 📊 문제 영향 범위

### Tier 3 커버리지 변화

| 버전 | 비즈니스 지표 템플릿 | Native Mode LLM | 커버리지 | 비고 |
|------|---------------------|----------------|---------|------|
| v7.4.0 | ✅ 8개 지표 | ❌ 없음 | 80-90% | 템플릿으로 대부분 커버 |
| v7.5.0 | ❌ 제거됨 | ❌ 없음 | **~0%** | **거의 모든 질문 실패** |
| 수정 필요 | ❌ 제거 유지 | ✅ 기본 모형 | 60-70% | 일반 Fermi 분해 |

### 실패하는 질문 유형

#### ❌ 현재 실패하는 질문들

1. **일반 Fermi 분해**
   - "서울-부산 이동 시간은?"
   - "여객기 한 대 안에 탁구공 몇 개?"
   - "서울 음식점 수는?"

2. **물리적/수학적 추정**
   - "피자 한 판 칼로리는?"
   - "한강 물의 양은?"
   - "지구에서 달까지 자동차로 몇 시간?"

3. **비즈니스 값 추정 (Quantifier 아닌 것)**
   - "ARPU는?" (단순 값 추정)
   - "Churn Rate는?" (단순 값 추정)

#### ✅ 정상 작동하는 경우

1. **Tier 1 매칭** (학습된 규칙)
2. **Tier 2 Source 조합** (데이터 있을 때)
3. **비즈니스 지표 계산** (Quantifier로 이동했으므로 OK)

---

## 💡 해결 방안

### 방안 1: 기본 Fermi 템플릿 추가 (권장 ⭐)

#### 개요
Native Mode에서도 간단한 규칙 기반 Fermi 모형을 생성

#### 구현
```python
# tier3.py - _generate_default_models()

def _generate_default_models(self, question: str, available: Dict, depth: int):
    """기본 템플릿 모형 생성"""
    
    # v7.5.0: 비즈니스 지표 템플릿 제거 (Quantifier가 처리)
    
    # 1. 일반 Fermi 템플릿 추가
    if self.llm_mode == 'native':
        logger.info(f"{'  ' * depth}    Native Mode: 기본 Fermi 템플릿 시도")
        
        # 간단한 규칙 기반 템플릿
        templates = self._match_fermi_templates(question, available)
        if templates:
            return templates
        
        # 템플릿 없으면 기본 모형 생성
        logger.info(f"{'  ' * depth}    기본 모형 생성 (단순 곱셈/나눗셈)")
        return self._create_simple_model(question, available)
    
    # 2. External Mode: LLM API
    elif self.llm_mode == 'external' and self.llm_client:
        return self._generate_llm_models(question, available, depth)
    
    # 3. Fallback
    return []

def _match_fermi_templates(self, question: str, available: Dict):
    """간단한 Fermi 템플릿 매칭"""
    
    # 템플릿 1: 시간 = 거리 / 속도
    if any(keyword in question for keyword in ['이동', '시간', '걸리']):
        if 'distance' in available or 'km' in question:
            return [self._create_time_distance_model(question, available)]
    
    # 템플릿 2: 개수 = 전체 / 단위
    if any(keyword in question for keyword in ['몇 개', '개수', '수는']):
        return [self._create_count_model(question, available)]
    
    # 템플릿 3: 총합 = 개수 × 단가
    if any(keyword in question for keyword in ['시장', '규모', '총']):
        return [self._create_market_size_model(question, available)]
    
    return []
```

#### 장점
- ✅ Native Mode에서도 기본적인 Fermi 분해 가능
- ✅ 비용 $0 유지
- ✅ 60-70% 커버리지 확보

#### 단점
- ⚠️ 템플릿 유지보수 필요
- ⚠️ 복잡한 질문은 여전히 실패 가능

---

### 방안 2: Phase 2 실패 시 Tier 2 Fallback

#### 개요
모형 생성 실패 시 바로 실패하지 않고 Tier 2로 넘김

#### 구현
```python
# tier3.py - estimate()

candidate_models = self._phase2_generate_models(...)

if not candidate_models:
    logger.warning(f"{'  ' * depth}  Phase 2 실패 (모형 없음)")
    logger.info(f"{'  ' * depth}  → Tier 2 Fallback 시도")
    
    # Tier 2로 Fallback
    return self.tier2.estimate(question, context or Context())
```

#### 장점
- ✅ 실패 케이스 감소
- ✅ Tier 2 Source 활용 (데이터 소싱)

#### 단점
- ⚠️ Tier 3의 재귀 분해 능력을 활용하지 못함
- ⚠️ Tier 2는 단순 조합만 가능

---

### 방안 3: Tier 1 규칙 파일 경로 수정

#### 문제
```python
# tier1.py
builtin_path = Path(__file__).parent.parent / "data" / "tier1_rules" / "builtin.yaml"
# → /umis_rag/data/tier1_rules/builtin.yaml (잘못됨)
```

#### 수정
```python
# tier1.py
builtin_path = Path(__file__).parent.parent.parent / "data" / "tier1_rules" / "builtin.yaml"
# → /data/tier1_rules/builtin.yaml (올바름)
```

#### 영향
- ✅ Tier 1 빌트인 규칙 사용 가능
- ⚠️ 하지만 현재는 파일 자체가 없음 (생성 필요)

---

## 🎯 권장 해결 순서

### 1단계: 기본 Fermi 템플릿 추가 (즉시)

**목표**: Native Mode에서도 기본적인 Fermi 분해 가능하게

**작업**:
1. `_match_fermi_templates()` 메서드 추가
2. 5-10개 기본 템플릿 구현:
   - 시간 = 거리 / 속도
   - 개수 = 전체 / 단위
   - 시장규모 = 고객수 × 단가
   - 부피 = 가로 × 세로 × 높이
   - 등등

**예상 효과**: 커버리지 0% → 60-70%

---

### 2단계: Phase 2 Fallback 구현 (단기)

**목표**: 완전 실패 방지

**작업**:
1. Phase 2 실패 시 Tier 2 호출
2. 적절한 에러 메시지 및 로깅

**예상 효과**: 실패율 감소, 사용자 경험 개선

---

### 3단계: Tier 1 경로 수정 + 규칙 파일 생성 (중기)

**목표**: Tier 1 효율성 향상

**작업**:
1. 경로 수정: `parent.parent.parent`
2. `data/tier1_rules/builtin.yaml` 생성
3. 초기 규칙 10-20개 추가

**예상 효과**: Tier 1 히트율 향상 (0% → 10-20%)

---

## 📝 테스트 케이스

### 수정 후 테스트해야 할 질문들

#### Tier 3 Fermi 분해 (일반)
```python
test_cases = [
    "서울에서 부산까지 이동할 때 걸리는 시간은?",
    "여객기 한 대 안에 탁구공을 가득 채우면 몇 개나 들어갈까?",
    "서울에 있는 음식점 수는 몇 개일까?",
    "피아노 하나의 무게는?",
    "한강의 물의 양은 몇 리터?",
]
```

#### 예상 결과
- ✅ 모두 Tier 3에서 처리 (템플릿 또는 기본 모형)
- ✅ 합리적인 분해 과정 (depth 1-2)
- ✅ 실행 시간 < 5초

---

## 📚 관련 파일

### 수정 필요한 파일
1. `umis_rag/agents/estimator/tier3.py` (주요)
   - Line 732-789: `_generate_default_models()`
   - Line 546-555: `estimate()` Phase 2 처리

2. `umis_rag/agents/estimator/tier1.py`
   - Line ~60: 빌트인 규칙 경로 수정

### 생성 필요한 파일
1. `data/tier1_rules/builtin.yaml`
   - 초기 Tier 1 규칙 정의

### 참고 파일
1. `config/llm_mode.yaml` (정책 문서)
2. `config/fermi_model_search.yaml` (Tier 3 설계)

---

## 🔄 변경 이력

### v7.5.0 (현재 - 문제 발생)
- ❌ 비즈니스 지표 템플릿 제거
- ❌ Native Mode에서 모형 생성 중단
- ❌ 커버리지 급감 (80% → 0%)

### v7.4.0 (이전 - 정상)
- ✅ 비즈니스 지표 템플릿 8개
- ✅ 템플릿 매칭으로 80-90% 커버
- ⚠️ 하지만 역할 혼재 (Estimator가 계산도 담당)

---

## 📌 결론

### 핵심 문제
**"Native Mode에서 Cursor에게 위임한다"는 설계 가정이 잘못됨**

- Cursor LLM은 Tier 3가 중단된 것을 알 수 없음
- "위임"이 자동으로 일어나지 않음
- 결과: 모든 질문이 실패

### 해결책
**Native Mode에서도 기본적인 Fermi 템플릿 제공**

- 5-10개 간단한 규칙 기반 템플릿
- Phase 2 실패 시 Tier 2 Fallback
- 비용 $0 유지
- 커버리지 60-70% 확보

### 우선순위
1. **즉시**: 기본 Fermi 템플릿 추가 ⭐⭐⭐
2. **단기**: Phase 2 Fallback 구현 ⭐⭐
3. **중기**: Tier 1 경로 수정 ⭐

---

**END OF REPORT**





