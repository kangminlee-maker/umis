# ========================================
# UMIS v7.11.1 - Cursor Rules
# ========================================

# ========================================
# 🚨 SYSTEM COMMANDS (필수!)
# ========================================

system_commands:
  "/umis setup"   - 설치 (2-3분, python setup/setup.py)
  "/umis status"  - 설치 상태 확인
  "/umis help"    - 도움말
  "/umis tools"   - 도구 목록 (19개)

# ========================================
# 🔍 INSTALLATION CHECK (자동)
# ========================================

first_action:
  when: "사용자 첫 요청 (시스템 명령어 제외)"
  check: "ls data/chroma_db/system_knowledge/ 2>&1"
  
  if_not_installed:
    message: |
      🚨 UMIS 미설치!
      
      다음 명령어를 입력해주세요:
      ```
      /umis setup
      ```
    block_requests: true
    allow_only: ["/umis", "!umis"]
  
  if_installed:
    proceed_to: "onboarding"

# ========================================
# 📚 ONBOARDING (설치 완료 후)
# ========================================

onboarding_sequence:
  step_1: "python3 scripts/query_system_rag.py tool:onboarding:quick_start"
  step_2: "python3 scripts/query_system_rag.py tool:onboarding:state_machine"
  step_3: "python3 scripts/query_system_rag.py tool:onboarding:agent_essentials"
  duration: "3분"
  result: "UMIS 전체 시스템 파악 완료"

# ========================================
# 🤖 AGENT MAPPING
# ========================================

agents:
  "@Observer | @Albert"   → tool:observer:complete
  "@Explorer | @Steve"    → tool:explorer:complete
  "@Quantifier | @Bill"   → tool:quantifier:complete
  "@Validator | @Rachel"  → tool:validator:complete
  "@Guardian | @Stewart"  → tool:guardian:complete
  "@Estimator | @Fermi"   → tool:estimator:complete

# ========================================
# 📖 DETAILED GUIDES
# ========================================

documentation:
  ai_reference: "UMIS_AI_REFERENCE.json (AI-only, minified, 35% smaller)"
  system_commands: "CURSOR_SYSTEM_COMMANDS.md (명령어 상세)"
  system_rag_index: "umis_core.yaml (19 tools INDEX)"
  full_spec: "umis.yaml (7,064 lines, complete)"

# ========================================
# END - 74 lines (360→74, 79% compression)
# ========================================
