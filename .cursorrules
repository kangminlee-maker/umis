# ========================================
# UMIS RAG Cursor Rules - AI Optimized
# v7.7.0 | Non-coder | Cursor-only
# ========================================

# === PART 1: UMIS 개념 (AI 필수 이해) ===

system:
  name: Universal Market Intelligence System
  version: 7.7.0
  
  agents:
    Observer: {name: Albert, role: market_structure, rag: true}       # v7.1.0+
    Explorer: {name: Steve, role: opportunity_discovery, rag: true}   # v7.0.0+
    Quantifier: {name: Bill, role: market_sizing, rag: true}         # v7.1.0+
    Validator: {name: Rachel, role: data_verification, rag: true}    # v7.1.0+
    Guardian: {name: Stewart, role: process_monitoring, rag: true}   # v7.1.0+ (Meta-RAG ✅)
    Estimator: {name: Fermi, role: value_estimation_5phase, rag: true}  # v7.7.0+ (5-Phase + Native 모드 ✅)
  
  # id: Agent ID (Observer, Explorer, ...)
  # name: Default name (Albert, Steve, ...) → agent_names.yaml로 커스터마이징
  
  flow: Observer(observe)→Explorer(rag+hypothesize)→Quantifier(calculate)→Validator(verify)→Guardian(approve)
  
  estimator_role: "협업 파트너 (모든 Agent가 필요 시 호출, 100% 커버리지)"
  
  discovery_sprint: 명확도<7 → 6-Agent 병렬 탐색 (Estimator 협업) → 목표 구체화
  
  rag:
    active: 6-Agent RAG (v7.7.0+)
    architecture: v3.0 (4-Layer: Canonical → Projected → Graph → Memory)
    
    layers:
      layer_1_canonical: "정규화된 원본"
      layer_2_projected: "Agent별 검색용 뷰"
      layer_3_graph: "Neo4j (13 노드, 45 관계)"
      layer_4_memory: "Query/Goal/RAE Memory"
    
    collections:
      explorer:
        - "explorer_knowledge_base (54개 패턴)"
        - "31 Business Models + 23 Disruption Patterns"
      
      quantifier:
        - "calculation_methodologies (30개, 빌드 필요)"
        - "market_benchmarks (100개, 빌드 필요)"
      
      validator:
        - "data_sources_registry (50개, 빌드 필요)"
        - "definition_validation_cases (84개, 빌드 필요)"
      
      observer:
        - "market_structure_patterns (30개, 빌드 필요)"
        - "value_chain_benchmarks (50개, 빌드 필요)"
      
      guardian:
        - "goal_memory (목표 정렬)"
        - "query_memory (순환 감지)"
        - "rae_index (평가 일관성)"
      
      estimator:
        - "learned_rules (0 → 2,000개 진화)"
        - "canonical_store (정규화)"
        - "estimator (Agent View)"
        - "data_sources_registry (24개, Validator 연계)" # v7.6.2+
      
      system:
        - "system_knowledge (31개 도구)" # ✅ 빌드 완료!
    
    sources: 
      - data/raw/umis_business_model_patterns.yaml (31 patterns)
      - data/raw/umis_disruption_patterns.yaml (23 patterns)
      - data/raw/market_benchmarks.yaml (추가 예정)
      - data/raw/definition_validation_cases.yaml (추가 예정)

user:
  skill: no-coding
  tool: Cursor Composer + Agent mode
  lang: 한국어
  workflow: 대화→자동화→결과

# === PART 2: 자동화 규칙 ===

## 초기 설치
setup:
  detect: ["umis 설치", "설정", "setup", "@setup"]
  
  flow:
    check_env:
      no: python setup/setup.py → 자동 설치
      yes: python setup/setup.py --check → 상태 확인
  
  commands:
    auto_install: python setup/setup.py
    minimal_install: python setup/setup.py --minimal
    check_status: python setup/setup.py --check
    build_index: python scripts/02_build_index.py --agent explorer
  
  guide: setup/AI_SETUP_GUIDE.md (AI 자동 설치 가이드)
  
  messages:
    start: |
      🚀 UMIS v7.7.0 자동 설치 시작
      
      setup/AI_SETUP_GUIDE.md 참조하여 진행합니다.
      소요 시간: 2-3분
    
    api_key: |
      ✅ .env 생성 완료!
      
      다음: .env 파일 열어서
      OPENAI_API_KEY=your-key → 자신의 키로 변경
      
      받기: https://platform.openai.com/api-keys
    
    ready: |
      ✅ 설치 완료!
      
      사용: Cmd+I → @umis.yaml
      "@Explorer, 시장 분석해줘"
      "@Fermi, LTV는?" ⭐ v7.7.0 (5-Phase + Native 모드!)

## Agent 이름 커스터마이징
agent_names:
  file: agent_names.yaml
  format: {observer: Albert, explorer: Steve, ...}
  bidirectional: true
  
  mapping:
    input: @Steve → Explorer
    output: Explorer → Steve
  
  defaults: {Observer: Albert, Explorer: Steve, Quantifier: Bill, Validator: Rachel, Guardian: Stewart}

## YAML 수정 → RAG 재구축
yaml_watch:
  files: [umis_business_model_patterns.yaml, umis_disruption_patterns.yaml, umis_ai_guide.yaml]
  
  on_save:
    ask: "RAG 재구축? (2초)"
    if_yes: python scripts/01_convert_yaml.py → python scripts/02_build_index.py --agent explorer
    msg: "✅ RAG 업데이트!"

## Explorer RAG 자동 활용
explorer_rag:
  pattern_search:
    detect: ["패턴 매칭", "트리거 시그널"]
    cmd: python scripts/query_rag.py pattern "{signals}"
    msg: "🔍 {pattern_id} 발견!"
  
  case_search:
    detect: ["유사 사례", "성공 사례"]
    cmd: python scripts/query_rag.py case "{industry}" --pattern {id}
    integrate: analysis

## 데이터 추가
data_add:
  detect: ["데이터 추가", "수정", "넣어줘"]
  
  flow:
    open_yaml → find_section → suggest_diff →
    if_approved: save → rebuild_rag → "✅ 완료!"
  
  example: "코웨이 해지율 추가" → umis_business_model_patterns.yaml 자동 수정

## 단축 명령
shortcuts:
  - "rag 검색 {q}": python scripts/query_rag.py {q}
  - "rag 재구축": scripts/01+02
  - "rag 통계": show collection stats
  - "rag 테스트": scripts/03_test_search.py

# === PART 3: 경로 & 설정 ===

paths:
  root: {workspace_root}
  setup: setup/
  scripts: scripts/
  data: data/
  docs: docs/
  dev_docs: dev_docs/
  projects: projects/
  
files:
  main_guide: umis.yaml
  architecture: UMIS_ARCHITECTURE_BLUEPRINT.md
  install: INSTALL.md
  schema: schema_registry.yaml
  
execution:
  cd: {root}
  venv: source venv/bin/activate (optional)
  relative: from root

# === PART 4: 메시지 템플릿 ===

messages:
  한국어: 모든 응답
  친화적: 기술용어 쉽게
  이모지: 최소 (필요시만)
  
  format:
    progress: "🔍 {action} 중..."
    success: "✅ {result}"
    error: "❌ {problem} → {solution}"

# === PART 5: 폴더 구조 (v7.0.0) ===

folders:
  setup: 설치 관련 (AI_SETUP_GUIDE.md, setup.py)
  umis_rag: RAG 코드 (실제 시스템)
  scripts: RAG 스크립트
  data: Vector DB + 패턴
  docs: 활성 프로토콜
  dev_docs: 개발 히스토리 (시스템 비의존)
  projects: 분석 프로젝트 (Git 제외)
  archive: Deprecated (main 브랜치에서 제외)

# === PART 6: Guardian Meta-RAG (v7.1.0) ===

guardian_meta_rag:
  enabled: true
  
  # 프로젝트 시작 시
  project_start:
    detect: ["프로젝트 시작", "[PROJECT_START]", "새 프로젝트"]
    
    action: |
      from umis_rag.guardian import GuardianMetaRAG
      
      guardian = GuardianMetaRAG()
      goal_id = guardian.set_goal("{project_goal}")
      
      print("✅ Guardian 활성화: 목표 설정 완료")
      print("  - 순환 패턴 감지")
      print("  - 목표 정렬 모니터링")
      print("  - 품질 자동 평가")
  
  # 쿼리 모니터링
  circular_detection:
    check: "매 쿼리마다"
    
    action: |
      result = guardian.memory.check_circular(query_text)
      
      if result['is_circular']:
          print("⚠️ 순환 감지: 같은 주제 3회 반복")
          print(f"💡 제안: {result['suggestion']}")
  
  # 산출물 평가
  deliverable_evaluation:
    detect: ["[DELIVERABLE_COMPLETE]", "산출물 완성"]
    
    action: |
      result = guardian.evaluate_deliverable({
          'id': deliverable_id,
          'content': content,
          'task_description': task
      })
      
      if result.passed:
          print(f"✅ 품질 검증 통과: {result.grade}")
      else:
          for warning in result.warnings:
              print(f"⚠️ {warning}")
          for rec in result.recommendations:
              print(f"💡 {rec}")

# ========================================
# PART 7: System RAG (Key-based) - AI 필수 실행!
# ========================================
#
# ⚠️ AI: 모든 UMIS 프로젝트 시작 시 이 섹션을 따르세요!
# ========================================

system_rag:
  enabled: true
  collection: "system_knowledge"
  total_tools: 44  # v7.7.0: 15개 Complete + 29개 Task
  
  purpose: |
    AI가 UMIS 전체 시스템을 빠르게 파악하고
    필요한 도구만 정확히 로드하여 컨텍스트 73-96% 절약
  
  v7_7_0_structure:
    tier_1_system: "9개 (umis.yaml 시스템 섹션, 0% 손실)"
    tier_2_complete: "6개 (Agent 전체 컨텍스트, 권장!)"
    tier_3_task: "29개 (세분화, 빠른 조회)"
    
    key_changes:
      - "umis.yaml 100% RAG 마이그레이션 (0% 손실)"
      - "Complete 도구 = umis.yaml 섹션 전체"
      - "실제 작업 = Complete 사용 (umis.yaml 참조 불필요!)"
      - "자동화 파이프라인 (sync_umis_to_rag.py)"
    
    usage_principle:
      - "⭐ 실제 작업: Tier 2 (Complete) 사용!"
      - "빠른 조회: Tier 3 (Task) 사용"
      - "시스템 이해: Tier 1 (System) 사용"

# ========================================
# AI 필수 실행 프로세스 (모든 프로젝트 시작 시!)
# ========================================

ai_mandatory_process:
  
  when_to_execute:
    - "프로젝트 시작 시 (첫 대화)"
    - "Agent 멘션 감지 시 (@Explorer, @Quantifier 등)"
    - "시장 분석 요청 시"
    - "복잡한 작업 시작 전"
  
  # ===== STEP 1: umis_core.yaml 로드 (필수!) =====
  step_1_load_index:
    action: "read_file tool 사용"
    file: "umis_core.yaml"
    reason: "3-Tier 구조 파악 및 도구 선택 (INDEX)"
    
    what_to_read:
      critical:
        - "ai_guide: 의도 → Tier 매핑"
        - "tools: 44개 도구 목록 (3-Tier)"
        - "usage_patterns: 사용 패턴 4개"
      
      optional:
        - "key_principles: 핵심 원칙"
    
    key_takeaways:
      - "44개 도구 (System:9, Complete:6, Task:29)"
      - "⭐ 실제 작업 = Complete 사용!"
      - "Complete = umis.yaml 참조 불필요"
    
    mandatory: true
    skip_condition: "none (항상 실행)"
  
  # ===== STEP 2: 쿼리 분석 + Tier 선택 =====
  step_2_parse_query_and_select_tier:
    action: "사용자 의도 파악 → 적절한 Tier 선택"
    
    intent_detection:
      system_understanding:
        keywords: ["시스템 구조", "워크플로우", "협업 방식"]
        tier: "Tier 1 (System)"
        tools: ["tool:system:system_architecture", "tool:system:implementation_guide"]
      
      agent_work:
        keywords: ["@Observer", "@Explorer", "시장 분석", "기회 발굴"]
        tier: "Tier 2 (Complete) ⭐ 권장!"
        tools: ["tool:observer:complete", "tool:explorer:complete"]
        note: "실제 작업 = 항상 Complete!"
      
      quick_check:
        keywords: ["방법만", "개념만", "빠르게"]
        tier: "Tier 3 (Task)"
        tools: ["tool:observer:market_structure", "tool:quantifier:sam_4methods"]
    
    agent_detection:
      - "@Observer" → tool:observer:complete (Complete!)
      - "@Explorer" → tool:explorer:complete (Complete!)
      - "@Quantifier" → tool:quantifier:complete (Complete!)
      - "@Validator" → tool:validator:complete (Complete!)
      - "@Guardian" → tool:guardian:complete (Complete!)
      - "@Fermi|@Estimator" → tool:estimator:complete (Complete!)
      
      - "시장 분석" → observer+explorer+quantifier complete
      - "기회 발굴" → explorer complete
      - "SAM 계산" → quantifier complete
      - "값 추정" → estimator complete
      
      tool_selection_v7_7_0:
        default_strategy: "실제 작업 = Complete, 빠른 조회 = Task"
        
        complete_tools:
          observer: "tool:observer:complete ⭐ (실제 작업 권장)"
          explorer: "tool:explorer:complete ⭐ (실제 작업 권장)"
          quantifier: "tool:quantifier:complete ⭐ (실제 작업 권장)"
          validator: "tool:validator:complete ⭐ (실제 작업 권장)"
          guardian: "tool:guardian:complete ⭐ (실제 작업 권장)"
          estimator: "tool:estimator:complete ⭐ (실제 작업 권장)"
        
        task_tools:
          explorer:
            "패턴만": "tool:explorer:pattern_search"
            "7단계만": "tool:explorer:7_step_process"
          
          quantifier:
            "SAM만": "tool:quantifier:sam_4methods"
            "성장만": "tool:quantifier:growth_analysis"
          
          observer:
            "구조만": "tool:observer:market_structure"
            "가치사슬만": "tool:observer:value_chain"
          
          validator:
            "정의만": "tool:validator:data_definition"
        
        system_tools:
          "시스템 구조": "tool:system:system_architecture"
          "구현 가이드": "tool:system:implementation_guide"
          "모든 Agent": "tool:system:agents (16,578 토큰, 주의!)"
  
  # ===== STEP 3: System RAG 검색 (필수!) =====
  step_3_execute_system_rag:
    action: "run_terminal_cmd tool 사용하여 필요한 도구 로드"
    
    command_template: "python3 scripts/query_system_rag.py {tool_key}"
    
    execution_examples:
      
      example_1_single_agent:
        query: "@Explorer, 구독 모델 패턴"
        tool_key: "tool:explorer:complete"
        tier: "Tier 2 (Complete)"
        command: "python3 scripts/query_system_rag.py tool:explorer:complete"
        expected_output: "Explorer 전체 컨텍스트 (~460줄, 14,237자)"
        context_total: "~200줄 (umis_core) + ~460줄 (complete) = ~660줄 (~15,000 토큰)"
        savings: "96%"
        note: "umis.yaml 참조 불필요!"
      
      example_2_market_analysis:
        query: "음악 스트리밍 시장 분석"
        tier: "Tier 2 (Complete)"
        tool_keys:
          - "tool:observer:complete"
          - "tool:explorer:complete"
          - "tool:quantifier:complete"
        commands:
          - "python3 scripts/query_system_rag.py tool:observer:complete"
          - "python3 scripts/query_system_rag.py tool:explorer:complete"
          - "python3 scripts/query_system_rag.py tool:quantifier:complete"
        expected_output: "3개 Agent 전체 컨텍스트"
        context_total: "~200줄 + ~1,000줄 = ~1,200줄 (~12,000 토큰)"
        savings: "70%"
        note: "3개 Agent 모두 전체 작업 방식으로 완벽한 협업!"
      
      example_3_workflow:
        query: "피아노 구독 서비스 Discovery Sprint"
        tool_keys:
          - "tool:framework:discovery_sprint"
            - "tool:observer:market_structure"
            - "tool:explorer:pattern_search"
            - "tool:quantifier:sam_4methods"
          - "tool:validator:data_definition"
        commands: "각 도구별 query_system_rag.py 실행"
        expected_output: "5개 도구 content (~2,500줄)"
        context_total: "709 + 2,500 = 3,209줄 (vs 6,102줄, 47% 절약)"
    
    mandatory: true
    reason: "도구 없이는 작업 불가능"
    fallback: "System RAG 실패 시 umis.yaml 전체 로드 (비효율)"
  
  # ===== STEP 4: Workflow 실행 =====
  step_4_execute_workflow:
    action: "로드된 도구 content 기반으로 작업 수행"
    
    workflow_types:
      
      discovery_sprint:
        when: "목표 불명확 (명확도 < 7)"
        tools:
            - "tool:framework:discovery_sprint"
          - "모든 Agent 도구 (5-Agent 병렬)"
        duration: "1-3일"
      
      comprehensive_study:
        when: "상세 분석 필요"
        tools:
          - "tool:observer:market_structure"
          - "tool:explorer:7_step_process"
          - "tool:quantifier:sam_4methods"
          - "tool:validator:data_definition"
        duration: "2-4주"
      
      rapid_assessment:
        when: "빠른 파악"
        tools:
          - "tool:observer:market_structure"
          - "tool:explorer:pattern_search"
          - "tool:universal:guestimation"
        duration: "1-3일"
      
      opportunity_discovery:
        when: "기회 발굴"
        tools:
          - "tool:explorer:pattern_search"
          - "tool:explorer:7_step_process"
          - "tool:explorer:validation_protocol"
        duration: "3-5일"

# ========================================
# AI 실행 체크리스트 (모든 프로젝트!)
# ========================================

ai_execution_checklist:
  
  before_starting_any_project:
    - step: "1. umis_core.yaml 읽기 (read_file)"
      file: "umis_core.yaml"
      focus: "Lines 40-110 (Quick Start + Agent Selection)"
      mandatory: true
    
    - step: "2. 쿼리 분석 (agent + keywords)"
      action: "사용자 요청에서 필요한 Agent와 도구 파악"
      output: "agent_id, tool_keys 리스트"
      mandatory: true
    
    - step: "3. System RAG 실행 (run_terminal_cmd)"
      action: "각 tool_key마다 query_system_rag.py 실행"
      command: "python3 scripts/query_system_rag.py {tool_key}"
      repeat: "필요한 모든 도구에 대해"
      mandatory: true
    
    - step: "4. 로드된 도구 content 활용"
      action: "System RAG 결과를 컨텍스트에 포함하여 작업 수행"
      mandatory: true
  
  failure_handling:
    if_system_rag_fails:
      - action: "umis.yaml 전체 로드 (fallback)"
      - warning: "컨텍스트 비효율 (6,102줄)"
      - notify_user: "System RAG 실패, 전체 파일 로드 중"
    
    if_collection_missing:
      - command: "python3 scripts/build_system_knowledge.py"
      - message: "System RAG 인덱스 구축 중 (1분)"
      - retry: "구축 후 다시 검색"

# ========================================
# AI 사용 예시 (따라하세요!)
# ========================================

ai_usage_examples:
  
  example_1_simple:
    user_query: "@Explorer, 구독 모델 패턴 찾아줘"
    
    ai_process:
      step_1: |
        # umis_core.yaml 읽기
        read_file("umis_core.yaml", offset=40, limit=70)
        → Quick Start 이해
      
      step_2: |
        # 쿼리 분석
        agent = "explorer"
        keywords = ["구독", "모델", "패턴"]
        tool_key = "tool:explorer:pattern_search"
      
      step_3: |
        # System RAG 실행
        run_terminal_cmd("python3 scripts/query_system_rag.py tool:explorer:pattern_search")
        
        결과: 도구 content (~400줄) 획득
      
      step_4: |
        # 작업 수행
        로드된 "tool:explorer:pattern_search" content 기반으로
        → RAG 패턴 검색 프로세스 이해
        → 실제 RAG 검색 실행
        → 구독 모델 패턴 분석
    
    context_used: "709 + 400 = 1,109줄 (vs 6,102줄, 82% 절약)"
  
  example_2_complex:
    user_query: "음악 스트리밍 시장 분석"
    
    ai_process:
      step_1: |
        # umis_core.yaml 읽기
        read_file("umis_core.yaml", offset=88, limit=110)
        → Decision Guide 참조
        → "시장 분석" = Observer → Explorer → Quantifier
      
      step_2: |
        # 필요한 도구 식별
        tool_keys = [
          "tool:observer:market_structure",
          "tool:explorer:pattern_search",
          "tool:quantifier:sam_4methods"
        ]
      
      step_3: |
        # System RAG 실행 (병렬 또는 순차)
        for key in tool_keys:
          run_terminal_cmd(f"python3 scripts/query_system_rag.py {key}")
        
        결과: 3개 도구 content (~1,200줄) 획득
      
      step_4: |
        # Workflow 실행
        1. Observer: 시장 구조 관찰 (market_structure 도구 활용)
        2. Explorer: 기회 발굴 (pattern_search 도구 + RAG)
        3. Quantifier: SAM 계산 (sam_4methods 도구)
    
    context_used: "709 + 1,200 = 1,909줄 (vs 6,102줄, 69% 절약)"
  
  example_3_discovery_sprint:
    user_query: "피아노 구독 서비스 시장 (목표 불명확)"
    
    ai_process:
      step_1: |
        # umis_core.yaml 읽기
        read_file("umis_core.yaml")
        → Discovery Sprint 필요 판단 (명확도 < 7)
      
      step_2: |
        # Discovery Sprint 도구 + 모든 Agent 도구
        tool_keys = [
          "tool:framework:discovery_sprint",
          "tool:observer:market_structure",
          "tool:explorer:pattern_search",
          "tool:explorer:7_step_process",
          "tool:quantifier:sam_4methods",
          "tool:validator:data_definition",
          "tool:guardian:progress_monitoring"
        ]
      
      step_3: |
        # System RAG 실행 (7개 도구)
        for key in tool_keys:
          run_terminal_cmd(f"python3 scripts/query_system_rag.py {key}")
      
      step_4: |
        # Discovery Sprint 실행
        → 6-Agent 병렬 탐색 (Estimator 협업 가능)
        → 목표 구체화
        → 다음 단계 결정
    
    context_used: "949 + 2,500 = 3,449줄 (vs 6,663줄, 48% 절약)"
  
  example_4_estimator:
    user_query: "@Fermi, SaaS LTV는?"
    
    ai_process:
      step_1: |
        # umis_core.yaml 읽기
        read_file("umis_core.yaml", offset=603, limit=90)
        → Estimator 섹션 파악
        → 5-Phase Architecture (v7.7.0: Phase + Step 명확화)
        → 12개 비즈니스 지표
      
      step_2: |
        # 도구 식별
        tool_key = "tool:estimator:estimate"
      
      step_3: |
        # System RAG 실행
        run_terminal_cmd("python3 scripts/query_system_rag.py tool:estimator:estimate")
        
        결과: Estimator 도구 content (~500줄) 획득
      
      step_4: |
        # EstimatorRAG 사용
        from umis_rag.agents.estimator import EstimatorRAG
        
        estimator = EstimatorRAG()
        result = estimator.estimate("SaaS LTV는?")
        
        # Phase 0 → 1 → 2 → 3 → 4 자동 시도
        # Phase 2 우선: Validator 검색 (85% 처리)
        # Phase 4: Fermi 분해, 재귀 추정, Backtracking
        # 결과: value, phase, decomposition
    
    context_used: "949 + 500 = 1,449줄 (vs 6,663줄, 78% 절약)"

# ========================================
# 자주 사용하는 도구 키 (Quick Reference)
# ========================================

common_tool_keys:
  
  explorer:
    - "tool:explorer:pattern_search (RAG 패턴 검색)"
    - "tool:explorer:7_step_process (7단계 기회 발굴)"
    - "tool:explorer:validation_protocol (가설 검증)"
    - "tool:explorer:hypothesis_generation (가설 생성)"
  
  quantifier:
    - "tool:quantifier:sam_4methods (SAM 4가지 방법)"
    - "tool:quantifier:growth_analysis (성장률 분석)"
    - "tool:quantifier:scenario_planning (시나리오)"
    - "tool:quantifier:benchmark_analysis (벤치마크)"
  
  validator:
    - "tool:validator:data_definition (데이터 정의 검증)"
    - "tool:validator:creative_sourcing (창의적 소싱)"
    - "tool:validator:gap_analysis (Gap 분석)"
    - "tool:validator:source_verification (출처 검증)"
  
  observer:
    - "tool:observer:market_structure (시장 구조)"
    - "tool:observer:value_chain (가치사슬)"
    - "tool:observer:inefficiency_detection (비효율 감지)"
    - "tool:observer:disruption_opportunity (파괴 기회)"
  
  guardian:
    - "tool:guardian:progress_monitoring (진행 모니터링)"
    - "tool:guardian:quality_evaluation (품질 평가)"
  
  estimator:
    - "tool:estimator:estimate (값 추정, 5-Phase)" ⭐ v7.7.0
    - "tool:estimator:cross_validation (교차 검증)"
    - "tool:estimator:learning_system (학습 시스템)"
  
  framework:
    - "tool:framework:13_dimensions (13차원 시장 정의)"
    - "tool:framework:discovery_sprint (Discovery Sprint)"
    - "tool:framework:7_powers (7 Powers)"
    - "tool:framework:counter_positioning (Counter-Positioning)"
  
  universal:
    - "tool:universal:guestimation (빠른 추정)"
    - "tool:universal:domain_reasoner_10_signals (정밀 분석)"
    - "tool:universal:hybrid_strategy (하이브리드)"

# ========================================
# 디버깅 명령 (System RAG 문제 시)
# ========================================

debug_commands:
  
  check_collection:
    command: "python3 scripts/query_system_rag.py --stats"
    output: "Collection 상태, Agent별 도구 수"
  
  list_all_keys:
    command: "python3 scripts/query_system_rag.py --list"
    output: "사용 가능한 모든 tool_key"
  
  rebuild_index:
    command: "python3 scripts/build_system_knowledge.py"
    when: "Collection 없음 또는 손상"
    duration: "1분"
  
  test_search:
    command: "python3 scripts/query_system_rag.py tool:explorer:pattern_search"
    purpose: "검색 테스트"

# ========================================
# END
# ========================================

