# ========================================
# UMIS RAG Cursor Rules - AI Optimized
# v7.0.0 | Non-coder | Cursor-only
# ========================================

# === PART 1: UMIS 개념 (AI 필수 이해) ===

system:
  name: Universal Market Intelligence System
  version: 7.0.0
  
  agents:
    Observer: {name: Albert, role: market_structure, rag: false}
    Explorer: {name: Steve, role: opportunity_discovery, rag: true}  # 유일!
    Quantifier: {name: Bill, role: market_sizing, rag: false}
    Validator: {name: Rachel, role: data_verification, rag: false}
    Guardian: {name: Stewart, role: process_monitoring, rag: false}
  
  # id: Agent ID (Observer, Explorer, ...)
  # name: Default name (Albert, Steve, ...) → agent_names.yaml로 커스터마이징
  
  flow: Observer(observe)→Explorer(rag+hypothesize)→Quantifier(calculate)→Validator(verify)→Guardian(approve)
  
  discovery_sprint: 명확도<7 → 5-Agent 병렬 탐색 → 목표 구체화
  
  rag:
    active: Explorer only
    architecture: v3.0 (Canonical → Projected → Graph)
    layers:
      vector: data/chroma/ (canonical_index, projected_index)
      graph: Neo4j (optional, 13 patterns, 45 relationships)
      memory: Query/Goal/RAE Memory
    sources: 
      - data/raw/umis_business_model_patterns.yaml (31 patterns)
      - data/raw/umis_disruption_patterns.yaml (23 patterns)

user:
  skill: no-coding
  tool: Cursor Composer + Agent mode
  lang: 한국어
  workflow: 대화→자동화→결과

# === PART 2: 자동화 규칙 ===

## 초기 설치
setup:
  detect: ["umis 설치", "설정", "setup", "@setup"]
  
  flow:
    check_env:
      no: python setup/setup.py → 자동 설치
      yes: python setup/setup.py --check → 상태 확인
  
  commands:
    auto_install: python setup/setup.py
    minimal_install: python setup/setup.py --minimal
    check_status: python setup/setup.py --check
    build_index: python scripts/02_build_index.py --agent explorer
  
  guide: setup/AI_SETUP_GUIDE.md (AI 자동 설치 가이드)
  
  messages:
    start: |
      🚀 UMIS v7.0.0 자동 설치 시작
      
      setup/AI_SETUP_GUIDE.md 참조하여 진행합니다.
      소요 시간: 2-3분
    
    api_key: |
      ✅ .env 생성 완료!
      
      다음: .env 파일 열어서
      OPENAI_API_KEY=your-key → 자신의 키로 변경
      
      받기: https://platform.openai.com/api-keys
    
    ready: |
      ✅ 설치 완료!
      
      사용: Cmd+I → @umis.yaml
      "@Explorer, 시장 분석해줘"

## Agent 이름 커스터마이징
agent_names:
  file: agent_names.yaml
  format: {observer: Albert, explorer: Steve, ...}
  bidirectional: true
  
  mapping:
    input: @Steve → Explorer
    output: Explorer → Steve
  
  defaults: {Observer: Albert, Explorer: Steve, Quantifier: Bill, Validator: Rachel, Guardian: Stewart}

## YAML 수정 → RAG 재구축
yaml_watch:
  files: [umis_business_model_patterns.yaml, umis_disruption_patterns.yaml, umis_ai_guide.yaml]
  
  on_save:
    ask: "RAG 재구축? (2초)"
    if_yes: python scripts/01_convert_yaml.py → python scripts/02_build_index.py --agent explorer
    msg: "✅ RAG 업데이트!"

## Explorer RAG 자동 활용
explorer_rag:
  pattern_search:
    detect: ["패턴 매칭", "트리거 시그널"]
    cmd: python scripts/query_rag.py pattern "{signals}"
    msg: "🔍 {pattern_id} 발견!"
  
  case_search:
    detect: ["유사 사례", "성공 사례"]
    cmd: python scripts/query_rag.py case "{industry}" --pattern {id}
    integrate: analysis

## 데이터 추가
data_add:
  detect: ["데이터 추가", "수정", "넣어줘"]
  
  flow:
    open_yaml → find_section → suggest_diff →
    if_approved: save → rebuild_rag → "✅ 완료!"
  
  example: "코웨이 해지율 추가" → umis_business_model_patterns.yaml 자동 수정

## 단축 명령
shortcuts:
  - "rag 검색 {q}": python scripts/query_rag.py {q}
  - "rag 재구축": scripts/01+02
  - "rag 통계": show collection stats
  - "rag 테스트": scripts/03_test_search.py

# === PART 3: 경로 & 설정 ===

paths:
  root: {workspace_root}
  setup: setup/
  scripts: scripts/
  data: data/
  docs: docs/
  dev_docs: dev_docs/
  projects: projects/
  
files:
  main_guide: umis.yaml
  architecture: UMIS_ARCHITECTURE_BLUEPRINT.md
  install: INSTALL.md
  schema: schema_registry.yaml
  
execution:
  cd: {root}
  venv: source venv/bin/activate (optional)
  relative: from root

# === PART 4: 메시지 템플릿 ===

messages:
  한국어: 모든 응답
  친화적: 기술용어 쉽게
  이모지: 최소 (필요시만)
  
  format:
    progress: "🔍 {action} 중..."
    success: "✅ {result}"
    error: "❌ {problem} → {solution}"

# === PART 5: 폴더 구조 (v7.0.0) ===

folders:
  setup: 설치 관련 (AI_SETUP_GUIDE.md, setup.py)
  umis_rag: RAG 코드 (실제 시스템)
  scripts: RAG 스크립트
  data: Vector DB + 패턴
  docs: 활성 프로토콜
  dev_docs: 개발 히스토리 (시스템 비의존)
  projects: 분석 프로젝트 (Git 제외)
  archive: Deprecated (main 브랜치에서 제외)

# === PART 6: Guardian Meta-RAG (v7.1.0) ===

guardian_meta_rag:
  enabled: true
  
  # 프로젝트 시작 시
  project_start:
    detect: ["프로젝트 시작", "[PROJECT_START]", "새 프로젝트"]
    
    action: |
      from umis_rag.guardian import GuardianMetaRAG
      
      guardian = GuardianMetaRAG()
      goal_id = guardian.set_goal("{project_goal}")
      
      print("✅ Guardian 활성화: 목표 설정 완료")
      print("  - 순환 패턴 감지")
      print("  - 목표 정렬 모니터링")
      print("  - 품질 자동 평가")
  
  # 쿼리 모니터링
  circular_detection:
    check: "매 쿼리마다"
    
    action: |
      result = guardian.memory.check_circular(query_text)
      
      if result['is_circular']:
          print("⚠️ 순환 감지: 같은 주제 3회 반복")
          print(f"💡 제안: {result['suggestion']}")
  
  # 산출물 평가
  deliverable_evaluation:
    detect: ["[DELIVERABLE_COMPLETE]", "산출물 완성"]
    
    action: |
      result = guardian.evaluate_deliverable({
          'id': deliverable_id,
          'content': content,
          'task_description': task
      })
      
      if result.passed:
          print(f"✅ 품질 검증 통과: {result.grade}")
      else:
          for warning in result.warnings:
              print(f"⚠️ {warning}")
          for rec in result.recommendations:
              print(f"💡 {rec}")

# ========================================
# END
# ========================================

